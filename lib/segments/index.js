"use strict";

/**
 * Segments Index
 *
 * @module      segments/index
 * @description Loads all segments via the `require-directory` module
 */
var segments = module.exports = require("require-directory")(module);
